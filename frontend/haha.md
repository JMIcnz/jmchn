# Deploy onto Cloudflare Pages

## after deployed, update the generated URL to workers -> wrangler.toml -> CORS_ORIGIN  
## or just comment out warngler.toml -> CORS_ORIGIN

## add jpg to frontend/src/images/
